<div class="list">
    {{#each this.getPlanets as |planet|}}
        <div {{on "click" (fn this.selectPlanet planet.id)}}>
            <PlanetItem @active={{planet.active}} test={{planet.active}} >{{planet.name}}</PlanetItem>
        </div>
    {{/each}}
</div>
